const initialState = {
	isFetching: false,
	result: [

	]
};

function testeReducer (state = initialState, action) {
	switch (action.type) {
		default:
			return state;
	}
}

export default testeReducer;
